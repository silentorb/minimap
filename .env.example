# Copy to .env for local manual play overrides (gitignored).
# Dotenv loads only during normal Godot play — never in unit/functional tests.
# Existing process environment variables are not overwritten.

# MINIMAP_SCENARIO=res://config/scenarios/default.json
# MINIMAP_WORLD_SEED=42
