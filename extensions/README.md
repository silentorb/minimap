# Extension libraries

Built extension DLLs (e.g. `CompuQuest.Minimap.dll`) are copied here on build for local load via `config/extensions.json`.

Binary outputs (`*.dll`, `*.pdb`) are gitignored; build `CompuQuest.Minimap` (or the solution) to populate this folder.
